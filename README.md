### `Mobile Developer` Enthusiast.

```hi.kt
fun main(){
  val name: String = "Amir Hamzah";
  var age: Int = 17;
}
```
```index.ts
interface Bio {
  name: string;
  age: number;
  isStudent?: boolean;
}

const print = (bio: Bio): void => {
  console.log(`Name: ${bio.name}`);
  console.log(`Age: ${bio.age}`);

  if (bio.isStudent !== undefined) {
    console.log(`Status: ${biodata.isStudent ? 'Student' : 'Keep learning'}`);
  }
}
```
